<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to external files directory -->
    <external-path
        name="external_files"
        path="." />
    <!-- Allow access to files in the app's private directory -->
    <files-path
        name="internal_files"
        path="." />
    <!-- Allow access to cached files -->
    <cache-path
        name="cache_files"
        path="." />
</paths>
